# 新闻获取优化报告

## 📋 问题分析

**用户反馈：**
- 东方财富获取新闻太少（只有16条）
- 新浪财经获取新闻不足（只有31条）
- 目标：两边各自都能获取到25条以上新闻
- 尽可能不要通过极速数据拿新闻

**根本原因：**
1. 新浪财经API限制：每次请求最多返回50条
2. 东方财富HTML解析可能不完整
3. 过滤条件太严格，筛掉了很多相关新闻
4. 请求数量不够

## ✅ 优化方案

### 1. 增加请求数量

**东方财富：**
- 原来：`limit * 2`（如limit=50，则请求100条）
- 现在：固定请求100条

**新浪财经：**
- 原来：300条（但API限制只返回50条）
- 现在：500条请求 + 多页请求机制

### 2. 多页请求机制（新浪财经）

由于新浪API每次最多返回50条，实现多页请求：
```typescript
const perPage = 50;
const pages = Math.ceil(limit / perPage);

for (let page = 1; page <= Math.min(pages, 10); page++) {
  // 请求第page页
  // 过滤相关新闻
  // 累积结果
}
```

### 3. 放宽过滤条件

**黄金新闻关键词扩展：**
```
原来：15个关键词
现在：25个关键词
新增：美元指数、实际利率、地缘政治、黄金ETF、金矿、
      黄金期货、黄金现货、gold、precious metal、bullion
```

**美股新闻关键词扩展：**
```
原来：18个关键词
现在：35个关键词
新增：S&P、Dow Jones、AI、人工智能、芯片、半导体、云计算、
      Meta、Netflix、Facebook、Intel、AMD、美国股市、美国市场
```

**A股新闻关键词扩展：**
```
原来：25个关键词
现在：45个关键词
新增：成交量、政策、监管、改革、开放、芯片、锂电、光伏、
      军工、消费、科技、主力、涨停、跌停、停牌、复牌、
      重组、牛市、熊市、震荡、反弹、回调
```

### 4. 优化重试机制

保持3次重试，每次15秒超时，确保新浪财经有足够时间响应。

## 📊 预期效果

### 优化前
- 东方财富：16条
- 新浪财经：31条
- 合计：47条
- 极速数据补充：需要

### 优化后（预期）
- 东方财富：25-30条（HTML解析优化）
- 新浪财经：30-40条（多页请求 + 放宽过滤）
- 合计：55-70条
- 极速数据补充：基本不需要

## 🔧 技术实现

### 修改的方法

1. `fetchNasdaqNewsHybrid()` - 纳斯达克新闻
   - 东方财富请求100条
   - 新浪财经请求500条（多页）

2. `fetchGoldNewsHybrid()` - 黄金新闻
   - 东方财富请求100条
   - 新浪财经请求500条（多页）

3. `fetchAStockNewsHybrid()` - A股新闻
   - 东方财富请求100条
   - 新浪财经请求500条（多页）

4. `fetchSinaGoldNews()` - 新浪黄金新闻
   - 实现多页请求机制
   - 扩展关键词
   - 智能停止（达到目标数量）

5. `fetchSinaUSStockNews()` - 新浪美股新闻
   - 扩展关键词
   - 放宽过滤条件

6. `fetchSinaAStockNews()` - 新浪A股新闻
   - 扩展关键词
   - 放宽过滤条件

## 📝 测试文件

- `test-news-optimization.js` - 测试优化效果
- `test-news-sources-detailed.js` - 详细测试各数据源

## ⚠️ 注意事项

1. **新浪API限制**
   - 每次请求最多50条
   - 需要多页请求才能获取更多

2. **请求频率**
   - 多页请求间隔500ms
   - 避免请求过快被限流

3. **极速数据**
   - 作为最后的补充手段
   - 只在前两个源不足时使用

## ✅ 优化完成

- ✅ 增加请求数量
- ✅ 实现多页请求机制（所有三个方法）
  - ✅ `fetchSinaGoldNews()` - 黄金新闻多页请求
  - ✅ `fetchSinaUSStockNews()` - 美股新闻多页请求
  - ✅ `fetchSinaAStockNews()` - A股新闻多页请求
- ✅ 扩展关键词列表
- ✅ 放宽过滤条件
- ✅ 优化重试机制
- ⏳ 待测试验证
- ⏳ 待推送生产环境

## 🔧 实现细节

### 多页请求机制

所有三个新浪财经新闻获取方法都实现了相同的多页请求逻辑：

```typescript
const perPage = 50;  // 新浪API每页最多50条
const pages = Math.ceil(limit / perPage);  // 计算需要请求的页数

for (let page = 1; page <= Math.min(pages, 10); page++) {
  // 请求第page页
  const response = await axios.get('/sina-news-proxy', {
    params: { 
      category: 'finance',
      num: perPage,
      page: page
    },
    timeout: 30000
  });
  
  // 过滤相关新闻
  // 累积结果
  
  // 如果已经足够，提前结束
  if (allNews.length >= limit) break;
  
  // 避免请求过快
  await this.sleep(500);
}
```

### 智能停止机制

- 当获取的新闻数量达到目标时，自动停止请求
- 避免不必要的API调用
- 提高响应速度

### 请求间隔

- 每页请求之间等待500ms
- 避免请求过快被限流
- 保护API稳定性
