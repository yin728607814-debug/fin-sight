# 快速导入指南

## 第一步：执行数据库迁移

在 Supabase SQL Editor 中执行：

```sql
-- 复制 database/migrations/003_add_astock_support.sql 的内容并执行
```

## 第二步：获取你的用户ID

1. 打开浏览器开发者工具（按 F12）
2. 切换到 Console（控制台）标签
3. 输入以下命令并回车：

```javascript
localStorage.getItem('userId')
```

4. 复制返回的用户ID（类似：`"a1b2c3d4-e5f6-7890-abcd-ef1234567890"`）

## 第三步：修改并执行导入脚本

1. 打开文件 `database/import_funds_data.sql`

2. 找到第 11 行，将 `'YOUR_USER_ID'` 替换为你的实际用户ID：

```sql
-- 修改前
v_user_id VARCHAR(255) := 'YOUR_USER_ID';

-- 修改后（示例）
v_user_id VARCHAR(255) := 'a1b2c3d4-e5f6-7890-abcd-ef1234567890';
```

3. 同时修改文件末尾的两处 `'YOUR_USER_ID'`（第 145 行和第 155 行）

4. 复制整个文件内容

5. 在 Supabase SQL Editor 中粘贴并执行

## 第四步：验证导入

执行完成后，你应该看到：

```
✓ 基金配置导入完成：纳斯达克 20 个，A股 10 个
✓ 持仓数据导入完成：纳斯达克 20 个，A股 10 个
```

然后访问：
- `/fund-config` - 应该看到 30 个基金（20个纳斯达克 + 10个A股）
- `/portfolio` - 应该看到 30 个持仓

## 导入的数据

### 纳斯达克基金（20个）
总投资：约 ¥277,450
总盈亏：约 -¥1,050

### A股基金（10个）
总投资：约 ¥29,900
总盈亏：约 +¥1,003

## 常见问题

### Q: 提示 "duplicate key value violates unique constraint"
A: 说明数据已经存在，可以忽略或先删除旧数据：
```sql
DELETE FROM positions WHERE user_id = 'YOUR_USER_ID';
DELETE FROM fund_configs WHERE user_id = 'YOUR_USER_ID';
```

### Q: 看不到导入的数据
A: 
1. 检查用户ID是否正确
2. 刷新页面（Ctrl+F5）
3. 检查浏览器控制台是否有错误

### Q: 如何删除导入的数据
A:
```sql
-- 删除持仓
DELETE FROM positions WHERE user_id = 'YOUR_USER_ID';

-- 删除基金配置
DELETE FROM fund_configs WHERE user_id = 'YOUR_USER_ID';
```

## 完成！

导入成功后，你可以：
1. 在投资组合页面查看所有持仓
2. 在基金配置页面管理基金列表
3. 添加新的持仓或编辑现有持仓
4. 查看收益统计和图表
