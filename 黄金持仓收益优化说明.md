# 黄金持仓收益优化说明

## 修改时间
2026-01-04

## 需求描述
优化黄金持仓功能，添加持仓收益和均价字段，并实现自动计算功能：
1. 添加黄金持仓时需要输入持仓收益和均价
2. 首次输入后，系统根据当前黄金价格和均价自动计算持仓收益
3. 均价按照人民币计算

## 修改内容

### 1. 添加持仓模态框 (`components/AddPositionModal.tsx`)

**新增字段：**
- `goldProfit`: 黄金持仓收益输入字段

**修改点：**
- 添加"持仓收益"输入框，支持正负数
- 添加表单验证，确保持仓收益必填
- 添加提示文字："首次输入后，系统将根据当前黄金价格和均价自动计算持仓收益"
- 保存时将用户输入的持仓收益存储到 `profitLoss` 字段

**代码示例：**
```typescript
// 黄金字段
const [goldGrams, setGoldGrams] = useState('');
const [goldInvestment, setGoldInvestment] = useState('');
const [goldProfit, setGoldProfit] = useState(''); // 新增

// 验证
if (goldProfit === '' || isNaN(parseFloat(goldProfit))) {
  newErrors.profit = '请输入持仓收益';
}

// 保存
const profit = parseFloat(goldProfit);
onAdd({
  assetType: 'gold',
  assetName: '现货黄金',
  quantity: grams,
  averageBuyPrice: averagePrice,
  investmentAmount: investment,
  profitLoss: profit, // 保存用户输入的收益
  createdAt: now,
  updatedAt: now
});
```

### 2. 编辑持仓模态框 (`components/EditPositionModal.tsx`)

**新增字段：**
- `goldProfit`: 黄金持仓收益编辑字段

**修改点：**
- 添加"持仓收益"输入框，支持编辑
- 加载持仓数据时，显示当前的持仓收益
- 添加提示文字："系统将根据当前黄金价格和均价自动计算持仓收益"
- 保存时更新持仓收益

**代码示例：**
```typescript
// 加载数据
useEffect(() => {
  if (position && position.assetType === 'gold') {
    setGoldGrams(position.quantity?.toString() || '');
    setGoldInvestment(position.investmentAmount.toString());
    setGoldProfit(position.profitLoss?.toString() || '0'); // 加载收益
  }
}, [position]);

// 保存
const profit = parseFloat(goldProfit);
onSave(position.id, {
  quantity: grams,
  averageBuyPrice: averagePrice,
  investmentAmount: investment,
  profitLoss: profit, // 更新收益
  updatedAt: new Date()
});
```

### 3. 投资组合服务 (`services/portfolioService.ts`)

**核心逻辑修改：**

在 `calculatePortfolio` 方法中，黄金持仓收益的计算逻辑：

```typescript
// 黄金：根据均价和当前价格计算收益
if (currentPrice !== undefined && position.quantity && position.averageBuyPrice) {
  // 当前市值 = 克数 × 当前价格（人民币/克）
  const positionValue = position.quantity * currentPrice;
  // 持仓收益 = 当前市值 - 持仓金额
  const profitLoss = positionValue - investment;
  const profitLossPercent = investment > 0 ? (profitLoss / investment) * 100 : 0;

  currentValue += positionValue;

  return {
    ...position,
    currentPrice,
    currentValue: positionValue,
    profitLoss, // 自动计算的收益
    profitLossPercent
  };
}

// 如果没有当前价格，使用用户输入的持仓收益
const positionValue = investment + (position.profitLoss || 0);
currentValue += positionValue;
return {
  ...position,
  currentValue: positionValue,
  profitLoss: position.profitLoss || 0, // 使用用户输入的收益
  profitLossPercent: investment > 0 ? ((position.profitLoss || 0) / investment) * 100 : 0
};
```

**计算逻辑说明：**

1. **有当前价格时（自动计算）：**
   - 当前市值 = 持仓克数 × 当前黄金价格（人民币/克）
   - 持仓收益 = 当前市值 - 持仓金额
   - 收益率 = (持仓收益 / 持仓金额) × 100%

2. **无当前价格时（使用用户输入）：**
   - 当前市值 = 持仓金额 + 用户输入的持仓收益
   - 持仓收益 = 用户输入的值
   - 收益率 = (持仓收益 / 持仓金额) × 100%

## 数据流程

### 添加黄金持仓流程：
1. 用户输入：克数、持仓金额、持仓收益
2. 系统计算：均价 = 持仓金额 / 克数
3. 系统保存：克数、均价、持仓金额、持仓收益（用户输入）
4. 页面刷新时：
   - 获取当前黄金价格（人民币/克）
   - 自动计算：持仓收益 = (克数 × 当前价格) - 持仓金额
   - 显示自动计算的收益

### 编辑黄金持仓流程：
1. 用户可以修改：克数、持仓金额、持仓收益
2. 系统重新计算：均价 = 持仓金额 / 克数
3. 系统更新：克数、均价、持仓金额、持仓收益
4. 页面刷新时：自动计算最新收益

## 价格转换说明

黄金价格转换由 `goldPriceConverter` 服务处理：
- API 返回：美元/盎司
- 系统转换：人民币/克
- 转换公式：人民币/克 = (美元/盎司 × 汇率) / 31.1035
- 默认汇率：7.2（可配置）

## 用户体验优化

1. **首次添加时：**
   - 用户需要手动输入持仓收益
   - 系统显示提示："首次输入后，系统将根据当前黄金价格和均价自动计算持仓收益"

2. **后续使用时：**
   - 系统自动根据当前价格计算收益
   - 用户可以在编辑时手动调整收益（如果需要）

3. **显示信息：**
   - 持仓克数
   - 持仓均价（人民币/克）
   - 持仓金额
   - 持仓收益（自动计算或用户输入）
   - 收益率

## 测试建议

1. **添加黄金持仓：**
   - 输入克数：10
   - 输入持仓金额：5200
   - 输入持仓收益：200
   - 验证均价显示：520元/克

2. **查看持仓：**
   - 验证当前价格是否正确显示（人民币/克）
   - 验证持仓收益是否自动更新
   - 验证收益率计算是否正确

3. **编辑持仓：**
   - 修改克数或持仓金额
   - 验证均价是否重新计算
   - 验证持仓收益是否可以手动调整

## 注意事项

1. **均价计算：**
   - 均价 = 持仓金额 / 克数
   - 均价单位：人民币/克
   - 均价在添加/编辑时自动计算

2. **收益计算：**
   - 首次添加时使用用户输入的收益
   - 后续刷新时使用自动计算的收益
   - 自动计算基于当前黄金价格（人民币/克）

3. **价格来源：**
   - 黄金价格来自 Investing.com API
   - 价格自动转换为人民币/克
   - 价格缓存时间：1分钟

## 相关文件

- `components/AddPositionModal.tsx` - 添加持仓模态框
- `components/EditPositionModal.tsx` - 编辑持仓模态框
- `services/portfolioService.ts` - 投资组合服务
- `services/goldPriceConverter.ts` - 黄金价格转换服务
- `hooks/usePriceDataWithConversion.ts` - 价格数据钩子（自动转换）
