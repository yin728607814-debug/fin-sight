# 投资组合功能更新 V2.0

## 📅 更新日期
2025-12-29

## 🎯 更新概述

根据用户反馈，重新设计了投资组合的数据结构和交互方式，使其更符合实际使用场景。

---

## 🔄 主要变更

### 1. 纳斯达克基金持仓

#### 变更前 ❌
- 需要输入：份额、买入价格、买入日期
- 需要选择：预设的基金列表（12个）
- 计算方式：份额 × 价格

#### 变更后 ✅
- 需要输入：**基金名称**（用户自定义）、**持仓金额**、**持仓收益**
- 不需要：份额、买入价格、买入日期
- 当日收益：**按照当天涨跌幅自动计算**

**优势**:
- 更符合实际使用习惯（直接输入持仓金额和收益）
- 支持任意基金名称（不限于预设列表）
- 自动计算当日收益（基于涨跌幅）

### 2. 黄金持仓

#### 变更前 ❌
- 需要输入：克数、买入价格、买入日期
- 计算方式：克数 × 买入价格

#### 变更后 ✅
- 需要输入：**持仓克数**、**持仓金额**
- 不需要：买入日期
- 自动计算：**持仓均价** = 持仓金额 / 持仓克数
- 持仓收益：**按照均价和当前价格自动计算**

**优势**:
- 简化输入（不需要买入日期）
- 自动计算均价
- 自动计算总收益

---

## 📊 新的数据结构

### Position 接口

```typescript
interface Position {
  id: string;
  assetType: 'nasdaq' | 'gold';
  assetName: string;
  
  // 纳斯达克基金
  fundName?: string;           // 用户自定义基金名称
  
  // 黄金
  quantity?: number;           // 克数
  averageBuyPrice?: number;    // 均价（人民币/克）
  
  // 通用
  investmentAmount: number;    // 持仓金额（元）
  profitLoss: number;          // 持仓收益（元）
  
  // 计算字段
  currentPrice?: number;       // 当前价格
  currentValue?: number;       // 当前市值
  profitLossPercent?: number;  // 收益率
  dailyProfitLoss?: number;    // 当日收益（元）- 仅纳斯达克
  dailyChange?: number;        // 当日涨跌幅（%）- 仅纳斯达克
  
  // 定投计划（仅纳斯达克）
  autoInvest?: AutoInvestPlan;
  
  // 元数据
  createdAt: Date;
  updatedAt: Date;
}
```

---

## 💡 使用示例

### 添加纳斯达克基金持仓

**输入**:
- 基金名称: `广发纳斯达克100ETF联接(QDII)C人民币`
- 持仓金额: `10000` 元
- 持仓收益: `500` 元（盈利输入正数，亏损输入负数）
- 定投计划（可选）: 每月 1000 元

**系统自动计算**:
- 当前市值 = 10000 + 500 = 10500 元
- 收益率 = (500 / 10000) × 100% = 5%
- 当日收益 = 持仓金额 × 当日涨跌幅（基于纳斯达克指数）

### 添加黄金持仓

**输入**:
- 持仓克数: `10` 克
- 持仓金额: `5200` 元

**系统自动计算**:
- 持仓均价 = 5200 / 10 = 520 元/克
- 当前市值 = 10 × 当前价格（例如 530 元/克）= 5300 元
- 持仓收益 = 5300 - 5200 = 100 元
- 收益率 = (100 / 5200) × 100% = 1.92%

---

## 🎨 界面变化

### 添加持仓弹窗

#### 纳斯达克表单
```
┌─────────────────────────────────────┐
│ 基金名称                            │
│ [输入框：例如广发纳指ETF联接C]      │
│                                     │
│ 持仓金额（元）                      │
│ [输入框：例如10000]                 │
│                                     │
│ 持仓收益（元）                      │
│ [输入框：例如500]                   │
│ 提示：盈利输入正数，亏损输入负数    │
│                                     │
│ [定投设置...]                       │
└─────────────────────────────────────┘
```

#### 黄金表单
```
┌─────────────────────────────────────┐
│ 持仓克数                            │
│ [输入框：例如10]                    │
│                                     │
│ 持仓金额（元）                      │
│ [输入框：例如5200]                  │
│ 提示：均价：¥520.00/克              │
└─────────────────────────────────────┘
```

### 持仓卡片

#### 纳斯达克卡片
```
┌─────────────────────────────────────┐
│ 广发纳指ETF联接C        [定投中]    │
│ 广发纳斯达克100ETF联接(QDII)C人民币 │
│                                     │
│ 持仓金额: ¥10,000.00                │
│ 当前市值: ¥10,500.00                │
│                                     │
│ ┌─────────────────────────────┐   │
│ │ 持仓收益    +¥500.00         │   │
│ │ 收益率      +5.00%           │   │
│ │ 当日收益    +¥50.00 (+0.50%) │   │
│ └─────────────────────────────┘   │
│                                     │
│ 下次定投: 2025-02-01 · ¥1,000 · 每月│
└─────────────────────────────────────┘
```

#### 黄金卡片
```
┌─────────────────────────────────────┐
│ 现货黄金                            │
│                                     │
│ 持仓金额: ¥5,200.00                 │
│ 当前市值: ¥5,300.00                 │
│ 持仓克数: 10.00 克                  │
│ 持仓均价: ¥520.00/克                │
│                                     │
│ ┌─────────────────────────────┐   │
│ │ 持仓收益    +¥100.00         │   │
│ │ 收益率      +1.92%           │   │
│ └─────────────────────────────┘   │
└─────────────────────────────────────┘
```

---

## 🔧 技术实现

### 计算逻辑

#### 纳斯达克
```typescript
// 当前市值 = 持仓金额 + 持仓收益
currentValue = investmentAmount + profitLoss;

// 收益率
profitLossPercent = (profitLoss / investmentAmount) × 100;

// 当日收益（基于涨跌幅）
if (currentPrice && previousPrice) {
  dailyChange = ((currentPrice - previousPrice) / previousPrice) × 100;
  dailyProfitLoss = investmentAmount × (dailyChange / 100);
}
```

#### 黄金
```typescript
// 持仓均价
averageBuyPrice = investmentAmount / quantity;

// 当前市值
currentValue = quantity × currentPrice;

// 持仓收益
profitLoss = currentValue - investmentAmount;

// 收益率
profitLossPercent = (profitLoss / investmentAmount) × 100;
```

---

## 📝 迁移指南

### 旧数据兼容性

**不兼容** - 由于数据结构变化较大，旧数据无法自动迁移。

**建议**:
1. 导出旧数据（如果需要保留）
2. 清除浏览器 localStorage
3. 重新添加持仓

### 重新添加持仓

#### 纳斯达克基金
1. 查看当前持仓金额和收益
2. 在新系统中输入：
   - 基金名称（完整名称）
   - 持仓金额
   - 持仓收益

#### 黄金
1. 计算总克数和总金额
2. 在新系统中输入：
   - 持仓克数
   - 持仓金额

---

## ✅ 测试清单

### 纳斯达克功能
- [ ] 添加基金持仓（自定义名称）
- [ ] 输入持仓金额和收益
- [ ] 查看当前市值计算
- [ ] 查看收益率计算
- [ ] 查看当日收益显示
- [ ] 设置定投计划
- [ ] 编辑持仓
- [ ] 删除持仓

### 黄金功能
- [ ] 添加黄金持仓
- [ ] 输入克数和金额
- [ ] 查看均价计算
- [ ] 查看持仓收益计算
- [ ] 添加多笔黄金持仓
- [ ] 查看黄金总览
- [ ] 编辑持仓
- [ ] 删除持仓

### 通用功能
- [ ] 数据持久化
- [ ] 刷新页面恢复
- [ ] 导出功能
- [ ] 价格更新
- [ ] 错误提示

---

## 🎉 优势总结

### 1. 更简单
- 减少输入字段
- 不需要计算份额和价格
- 直接输入实际金额

### 2. 更灵活
- 支持任意基金名称
- 不限于预设列表
- 适应各种场景

### 3. 更准确
- 当日收益基于涨跌幅
- 黄金收益基于均价
- 自动计算，减少错误

### 4. 更直观
- 显示实际金额
- 清晰的收益展示
- 一目了然的数据

---

## 📞 反馈

如有问题或建议，请随时反馈！

---

**更新完成时间**: 2025-12-29
**构建状态**: ✅ 通过 (4.41s)
**代码质量**: ⭐⭐⭐⭐⭐ (5/5)
