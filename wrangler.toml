# Cloudflare Pages 配置文件

name = "investment-portfolio"
compatibility_date = "2024-01-01"

# Pages 配置
pages_build_output_dir = "dist"

# 环境变量（在 Cloudflare Dashboard 中设置实际值）
[env.production]
# GEMINI_API_KEY = "your-api-key-here"  # 在 Dashboard 中设置

[env.preview]
# GEMINI_API_KEY = "your-api-key-here"  # 在 Dashboard 中设置
