# API配额优化说明

## 已完成的优化

### 1. 更新API密钥
- ✅ 已更新 `.env` 文件
- ✅ 已更新 `netlify-env-import.txt` 文件
- 新密钥：`AIzaSyDwPZDAO4HkB2kvPfbfPc35DZCZGEpHJYM`

### 2. 增加缓存时间
- **分析服务缓存**：30分钟 → **2小时**
- **新闻服务缓存**：5分钟 → **15分钟**
- **效果**：大幅减少重复API调用

### 3. 批量处理优化
- ✅ 50条新闻用1次API调用分析（vs之前50次）
- ✅ 50条新闻用1次API调用翻译（vs之前100次）
- **节省**：每次页面加载从150次调用减少到2次

## 配额使用估算

### 免费版配额
- **每分钟**：15次请求
- **每天**：1500次请求

### 当前使用情况
每次用户访问页面：
- **纳斯达克页面**：2次API调用（翻译1次 + 分析1次）
- **黄金页面**：1次API调用（分析1次）

### 理论支持用户数
- **有缓存**：每天可支持约750次页面访问（1500÷2）
- **无缓存**：每天可支持约500次页面访问

## 下一步优化建议

### 短期优化（可立即实施）
1. **添加浏览器本地存储**
   - 使用localStorage缓存分析结果
   - 即使刷新页面也不会重新调用API
   - 可节省50%以上的API调用

2. **减少新闻数量**
   - 从50条减少到30条
   - 分析速度更快，配额消耗更少

3. **智能刷新**
   - 添加"手动刷新"按钮
   - 默认使用缓存数据
   - 只在用户主动刷新时才调用API

### 长期优化（需要更多开发）
1. **集成备用AI服务**
   - 主要使用Gemini
   - 配额用完后自动切换到Groq（免费额度更高）
   - 实现无缝降级

2. **服务端缓存**
   - 在Netlify Functions中添加缓存
   - 多个用户共享同一份分析结果
   - 可节省90%以上的API调用

3. **按需加载**
   - 首次只加载10条新闻
   - 用户滚动时再加载更多
   - 减少不必要的API调用

## 更新Netlify环境变量

请按以下步骤更新Netlify环境变量：

1. 访问：https://app.netlify.com/
2. 选择你的项目
3. 进入：Site settings → Environment variables
4. 点击"Import from a .env file"
5. 复制 `netlify-env-import.txt` 的内容并粘贴
6. 点击"Import variables"
7. 重新部署网站

或者手动更新：
- 找到 `GEMINI_API_KEY` 和 `VITE_GEMINI_API_KEY`
- 更新为：`AIzaSyDwPZDAO4HkB2kvPfbfPc35DZCZGEpHJYM`

## 监控配额使用

你可以在Google Cloud Console查看配额使用情况：
1. 访问：https://console.cloud.google.com/
2. 选择你的项目
3. 进入：APIs & Services → Dashboard
4. 查看Gemini API的使用统计

## 注意事项

⚠️ **重要提醒**：
- 新密钥也有每天1500次的限制
- 建议实施上述优化措施
- 考虑添加备用AI服务
- 监控每日配额使用情况

## 需要帮助？

如果配额再次用完，可以考虑：
1. 实施上述短期优化
2. 集成Groq作为备用（免费额度：14,400次/天）
3. 创建另一个Google Cloud项目作为备用
